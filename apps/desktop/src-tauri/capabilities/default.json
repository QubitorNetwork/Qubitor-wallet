{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Quanta Wallet desktop core permissions. The vault_* commands are exposed via the Rust invoke_handler and need no plugin permission; only default core + shell-open are granted.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
